// Logical operators

var x = 10
var y =20

// AND operator
console.log(x > 30 && y < 30)

// OR operator
console.log(x > 30 || y < 30)

// Ternary (Conditional) operator

// varname = (condition) ? value1 : value2

var age = 18
var allowedToDrive = age >= 18 ? 'You are allowed to drive' : 'NOT allowed to drive'
console.log(allowedToDrive)